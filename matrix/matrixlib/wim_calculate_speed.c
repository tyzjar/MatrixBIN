#include <stdio.h>

#include "interface_matrixlib.h"

/*
 * int wimCalculateSpeed( VDB *pVDB)
 */
int wimCalculateSpeed( VDB *pVDB)
{
	fprintf( stdout, "wimCalculateSpeed: %p\n", (void*)pVDB);

	int iRc = MATRIXLIB_OK;

	return iRc;
}
