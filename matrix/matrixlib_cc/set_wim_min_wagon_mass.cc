#include <stdio.h>

#include "interface_matrixlib.h"

/*
 * int setWimMinWagonMass( VDB *pVDB)
 */
int setWimMinWagonMass( VDB *pVDB)
{
	fprintf( stdout, "setWimMinWagonMass: %p\n", (void*)pVDB);

	int iRc = MATRIXLIB_OK;

	return iRc;
}
