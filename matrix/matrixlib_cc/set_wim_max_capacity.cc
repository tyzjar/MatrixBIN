#include <stdio.h>

#include "interface_matrixlib.h"

/*
 * int setWimMaxCapacity( VDB *pVDB)
 */
int setWimMaxCapacity( VDB *pVDB)
{
	fprintf( stdout, "setWimMaxCapacity: %p\n", (void*)pVDB);

	int iRc = MATRIXLIB_OK;

	return iRc;
}
