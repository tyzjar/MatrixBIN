#include <stdio.h>

#include "interface_matrixlib.h"

/*
 * int setWimMinSpeed( VDB *pVDB)
 */
int setWimMinSpeed( VDB *pVDB)
{
	fprintf( stdout, "setWimMinSpeed: %p\n", (void*)pVDB);

	int iRc = MATRIXLIB_OK;

	return iRc;
}
