#include <stdio.h>

#include "interface_matrixlib.h"

/*
 * int setWimMaxTransitSpeed( VDB *pVDB)
 */
int setWimMaxTransitSpeed( VDB *pVDB)
{
	fprintf( stdout, "setWimMaxTransitSpeed: %p\n", (void*)pVDB);

	int iRc = MATRIXLIB_OK;

	return iRc;
}
