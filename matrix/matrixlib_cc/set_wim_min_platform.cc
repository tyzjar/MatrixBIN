#include <stdio.h>

#include "interface_matrixlib.h"

/*
 * int setWimMinlatform( VDB *pVDB)
 */
int setWimMinPlatform( VDB *pVDB)
{
	fprintf( stdout, "setWimMinPlatform: %p\n", (void*)pVDB);

	int iRc = MATRIXLIB_OK;

	return iRc;
}
