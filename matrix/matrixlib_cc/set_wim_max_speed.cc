#include <stdio.h>

#include "interface_matrixlib.h"

/*
 * int setWimMaxSpeed( VDB *pVDB)
 */
int setWimMaxSpeed( VDB *pVDB)
{
	fprintf( stdout, "setWimMaxSpeed: %p\n", (void*)pVDB);

	int iRc = MATRIXLIB_OK;

	return iRc;
}
